import React from 'react';

import './loader.scss';

const Loader = () => (
  <div className="loader">
    <p className="loader-text">
      Chargement en cours...
    </p>
  </div>

);

export default Loader;
